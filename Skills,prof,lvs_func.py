# define function lv(level,skills,proficiencies,dex,con,int,cha,str,wis): 
   # new_lv = ask ("what do you wan toyu new lv to be? \n")
#   lv_dif = new_lv - lv
#   if lv_dif > 0
        # lv_diff = lv_diff*2
   #     display you need to add {lv_dif}
   #    stat = display where do you want to add it?
#       num_stat = how many points?
        #lv_diff = lv_diff - num_stat
        #IF STAT == WIS
         #   wis = wis + num_stat
        
        #IF STAT == str

        #IF STAT == dex

        #IF STAT == con

        #IF STAT == cha

        #IF STAT == int
    
    #lv = new_lv

